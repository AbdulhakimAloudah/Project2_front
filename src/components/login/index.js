import React from "react";

const Login = () => {
  return (
    <div>
      <h1>hi king login</h1>
      <form action="">
        <label htmlFor="">email</label>
        <input type="email" />

        <label htmlFor="">password</label>
        <input type="password" />

        <button>login</button>
      </form>
    </div>
  );
};

export default Login;
